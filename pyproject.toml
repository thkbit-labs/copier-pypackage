[project]
name = "copier-pypackage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "thkbitlabs", email = "thkbitlabs@gmail.com" }]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
  "mypy>=1.16.1",
  "pre-commit>=4.2.0",
  "pytest>=8.4.1",
  "pytest-benchmark>=5.1.0",
  "pytest-cov>=6.2.1",
  "pytest-mock>=3.14.1",
  "pytest-pretty>=1.3.0",
  "ruff>=0.12.3",
]
